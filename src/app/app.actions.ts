import { createAction } from '@ngrx/store';

export const startGame = createAction('[Start-game Component] Start game');
export const finishGame = createAction('[Finish-game Component] Finish game');
